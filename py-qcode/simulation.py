

class Simulation():     
    """
    `Simulation` is the top-level class
    for py-qcode, the user is meant to set up, execute and save results
    from these objects.

    Each `Simulation` has the following attributes:
    ``
    """
    def __init__(self, arg):
        self.arg = arg
        